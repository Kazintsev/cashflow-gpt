# Справочник базы данных

> ID: schema_reference · Для: developer, agent · Тип: observed_snapshot
>
> Поля, типы, связи и ограничения. [Карта документации](index.md) · [Манифест](manifest.json)

Сформирован из каталога PostgreSQL 7 сентября 2026 года. Описывает только схему `public`. Не содержит строк прикладных данных. Типы, NULL, identity и ограничения взяты из каталога. Индексы, не представленные ограничениями, последовательности и серверные настройки не включены: этот справочник не является полным DDL-экспортом.

## Таблицы

| Таблица | Назначение |
|---|---|
| `account_balance_snapshots` | Контрольные остатки счетов на точный момент времени. |
| `accounts` | Счета денег, кредитных карт и платёжных буферов. Тип определяет участие в cash и семантику платежа. |
| `cash_flow_event_funding` | Распределение суммы фактической операции на событие с отношением funding или payment. |
| `cash_flow_events` | Датированные планы и состояния доходов, расходов и выплат по долгам. |
| `cash_flow_rules` | Источники повторяющихся событий: фиксированные платежи и зарплатные правила. |
| `categories` | Категории операций с возможной иерархией через parent_id. |
| `debt_goals` | Цели и приоритеты погашения; сами по себе не являются денежными операциями. |
| `debt_payment_details` | Фактическая разбивка платежа на тело, проценты и комиссию. |
| `finance_settings` | Ключи конфигурации со значениями JSONB. |
| `liabilities` | Описание долга и связь с кредитным счётом либо платёжным буфером. |
| `liability_balance_snapshots` | Исходные/контрольные суммы тела долга, процентов и комиссий на момент времени. |
| `liability_movements` | Движения тела долга, процентов и комиссий; направление задаётся отдельно. |
| `liability_payment_schedule` | Банковский график с разбивкой каждого платежа. |
| `living_budgets` | Индивидуальный лимит конкретной недели. |
| `planned_purchases` | Плановые покупки и их аналитический резерв. |
| `receipt_items` | Позиции чека. |
| `receipts` | Чек, связанный с расходной операцией. |
| `transaction_category_allocations` | Разбиение одного расхода по категориям. |
| `transactions` | Фактические перемещения денег с временной, бюджетной и зарплатной разметкой. |

<a id="table-account-balance-snapshots"></a>

## account_balance_snapshots

Контрольные остатки счетов на точный момент времени.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('balance_snapshots_id_seq'::regclass)` |
| `account_id` | `bigint` | нет | — |
| `balance_date` | `date` | нет | — |
| `balance` | `numeric` | нет | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `balance_as_of` | `timestamp with time zone` | нет | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `account_balance_snapshots_account_time_key` | UNIQUE | `UNIQUE (account_id, balance_as_of)` |
| `balance_snapshots_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE CASCADE` |
| `balance_snapshots_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |

<a id="table-accounts"></a>

## accounts

Счета денег, кредитных карт и платёжных буферов. Тип определяет участие в cash и семантику платежа.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `name` | `text` | нет | — |
| `account_type` | `text` | нет | — |
| `currency` | `text` | нет | `'RUB'::text` |
| `institution` | `text` | да | — |
| `credit_limit` | `numeric(14,2)` | да | — |
| `opening_balance` | `numeric(14,2)` | нет | `0` |
| `opening_balance_date` | `date` | да | — |
| `is_active` | `boolean` | нет | `true` |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `accounts_account_type_check` | CHECK | `CHECK ((account_type = ANY (ARRAY['cash'::text, 'bank'::text, 'credit_card'::text, 'loan'::text, 'mortgage'::text, 'savings'::text, 'investment'::text, 'other_asset'::text, 'other_liability'::text, 'payment_buffer'::text])))` |
| `accounts_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `accounts_rub` | CHECK | `CHECK ((currency = 'RUB'::text))` |
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

<a id="view-balance-snapshots"></a>

## balance_snapshots

Представление совместимости для снимков остатков счетов.

Представление; не отдельная таблица. Выходные поля:

| Поле | SQL-тип |
|---|---|
| `id` | `bigint` |
| `account_id` | `bigint` |
| `balance_date` | `date` |
| `balance` | `numeric` |
| `notes` | `text` |
| `created_at` | `timestamp with time zone` |
| `balance_as_of` | `timestamp with time zone` |

<a id="table-cash-flow-event-funding"></a>

## cash_flow_event_funding

Распределение суммы фактической операции на событие с отношением funding или payment.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('cash_flow_event_funding_id_seq'::regclass)` |
| `cash_flow_event_id` | `bigint` | нет | — |
| `transaction_id` | `bigint` | нет | — |
| `amount` | `numeric` | нет | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `relation_type` | `text` | нет | `'funding'::text` |
| `allocation_source` | `text` | нет | `'manual'::text` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `cash_flow_event_funding_allocation_source_check` | CHECK | `CHECK ((allocation_source = ANY (ARRAY['manual'::text, 'auto_card_minimum'::text, 'auto_installment'::text, 'auto_payment_buffer'::text])))` |
| `cash_flow_event_funding_amount_check` | CHECK | `CHECK ((amount > (0)::numeric))` |
| `cash_flow_event_funding_cash_flow_event_id_fkey` | FOREIGN KEY | `FOREIGN KEY (cash_flow_event_id) REFERENCES cash_flow_events(id) ON DELETE CASCADE` |
| `cash_flow_event_funding_cash_flow_event_id_transaction_id_key` | UNIQUE | `UNIQUE (cash_flow_event_id, transaction_id)` |
| `cash_flow_event_funding_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `cash_flow_event_funding_relation_type_check` | CHECK | `CHECK ((relation_type = ANY (ARRAY['funding'::text, 'payment'::text])))` |
| `cash_flow_event_funding_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE CASCADE` |
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

<a id="view-cash-flow-event-settlement-summary"></a>

## cash_flow_event_settlement_summary

Представление расчётных сумм исполнения и обеспечения события.

Представление; не отдельная таблица. Выходные поля:

| Поле | SQL-тип |
|---|---|
| `cash_flow_event_id` | `bigint` |
| `planned_amount` | `numeric` |
| `paid_amount` | `numeric` |
| `funded_amount` | `numeric` |
| `remaining_payment_amount` | `numeric` |
| `remaining_funding_amount` | `numeric` |

<a id="view-cash-flow-event-state"></a>

## cash_flow_event_state

Расчётное состояние событий для текущего момента.

Представление; не отдельная таблица. Выходные поля:

| Поле | SQL-тип |
|---|---|
| `event_id` | `bigint` |
| `event_date` | `date` |
| `event_type` | `text` |
| `amount` | `numeric` |
| `description` | `text` |
| `account_id` | `bigint` |
| `liability_id` | `bigint` |
| `status` | `text` |
| `paid_amount` | `numeric` |
| `funded_amount` | `numeric` |
| `cash_committed_amount` | `numeric` |
| `remaining_payment_amount` | `numeric` |
| `cash_still_required` | `numeric` |
| `derived_status` | `text` |

<a id="table-cash-flow-events"></a>

## cash_flow_events

Датированные планы и состояния доходов, расходов и выплат по долгам.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('cash_flow_events_id_seq'::regclass)` |
| `event_date` | `date` | нет | — |
| `event_type` | `text` | нет | — |
| `amount` | `numeric` | нет | — |
| `description` | `text` | нет | — |
| `account_id` | `bigint` | да | — |
| `liability_id` | `bigint` | да | — |
| `is_planned` | `boolean` | нет | `true` |
| `is_secured` | `boolean` | нет | `false` |
| `source_transaction_id` | `bigint` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `rule_id` | `bigint` | да | — |
| `period_start` | `date` | да | — |
| `period_end` | `date` | да | — |
| `calculation_method` | `text` | да | — |
| `living_budget_exempt` | `boolean` | нет | `false` |
| `security_amount` | `numeric` | да | — |
| `status` | `text` | нет | `'planned'::text` |
| `actual_amount` | `numeric` | да | — |
| `actual_date` | `date` | да | — |
| `source_type` | `text` | нет | `'manual'::text` |
| `source_key` | `text` | да | — |
| `fulfillment_mode` | `text` | нет | `'dated_payment'::text` |
| `liability_payment_schedule_id` | `bigint` | да | — |
| `income_completed_at` | `timestamp with time zone` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `cash_flow_events_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (account_id) REFERENCES accounts(id)` |
| `cash_flow_events_fulfillment_mode_check` | CHECK | `CHECK ((fulfillment_mode = ANY (ARRAY['dated_payment'::text, 'cumulative_contributions'::text])))` |
| `cash_flow_events_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id)` |
| `cash_flow_events_liability_payment_schedule_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_payment_schedule_id) REFERENCES liability_payment_schedule(id) ON DELETE SET NULL` |
| `cash_flow_events_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `cash_flow_events_rule_id_fkey` | FOREIGN KEY | `FOREIGN KEY (rule_id) REFERENCES cash_flow_rules(id)` |
| `cash_flow_events_source_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (source_transaction_id) REFERENCES transactions(id) ON DELETE SET NULL` |
| `cash_flow_events_source_type_check` | CHECK | `CHECK ((source_type = ANY (ARRAY['manual'::text, 'rule'::text, 'split_schedule'::text, 'legacy'::text, 'bank_schedule'::text, 'card_minimum'::text])))` |
| `cash_flow_events_status_check` | CHECK | `CHECK ((status = ANY (ARRAY['planned'::text, 'partially_secured'::text, 'secured'::text, 'partially_executed'::text, 'executed'::text, 'cancelled'::text])))` |
| `cash_flow_events_valid_amount` | CHECK | `CHECK (((amount >= (0)::numeric) AND (amount <> ALL (ARRAY['NaN'::numeric, 'Infinity'::numeric, '-Infinity'::numeric]))))` |
| `cash_flow_events_valid_type` | CHECK | `CHECK ((event_type = ANY (ARRAY['income'::text, 'expense'::text, 'debt_payment'::text, 'planned_expense'::text])))` |
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

<a id="table-cash-flow-rules"></a>

## cash_flow_rules

Источники повторяющихся событий: фиксированные платежи и зарплатные правила.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('cash_flow_rules_id_seq'::regclass)` |
| `name` | `text` | нет | — |
| `rule_type` | `text` | нет | — |
| `amount` | `numeric` | да | — |
| `payment_day` | `smallint` | да | — |
| `share_of_monthly_salary` | `numeric` | да | — |
| `weekend_rule` | `text` | да | — |
| `start_date` | `date` | да | — |
| `end_date` | `date` | да | — |
| `is_active` | `boolean` | нет | `true` |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `event_type` | `text` | да | — |
| `schedule_type` | `text` | нет | `'monthly'::text` |
| `liability_id` | `bigint` | да | — |
| `account_id` | `bigint` | да | — |
| `calculation_method` | `text` | да | — |
| `currency` | `text` | нет | `'RUB'::text` |
| `living_budget_exempt` | `boolean` | нет | `true` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `cash_flow_rules_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE SET NULL` |
| `cash_flow_rules_event_type_check` | CHECK | `CHECK (((event_type IS NULL) OR (event_type = ANY (ARRAY['income'::text, 'expense'::text, 'debt_payment'::text]))))` |
| `cash_flow_rules_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE SET NULL` |
| `cash_flow_rules_name_key` | UNIQUE | `UNIQUE (name)` |
| `cash_flow_rules_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `cash_flow_rules_rub` | CHECK | `CHECK ((currency = 'RUB'::text))` |
| `cash_flow_rules_rule_type_check` | CHECK | `CHECK ((rule_type = ANY (ARRAY['fixed'::text, 'salary_advance'::text, 'salary_balance'::text, 'recurring_obligation'::text, 'salary'::text, 'living_budget'::text])))` |
| `cash_flow_rules_schedule_type_check` | CHECK | `CHECK ((schedule_type = ANY (ARRAY['monthly'::text, 'weekly'::text])))` |

<a id="table-categories"></a>

## categories

Категории операций с возможной иерархией через parent_id.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `name` | `text` | нет | — |
| `category_type` | `text` | нет | — |
| `parent_id` | `bigint` | да | — |
| `is_active` | `boolean` | нет | `true` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `categories_category_type_check` | CHECK | `CHECK ((category_type = ANY (ARRAY['income'::text, 'expense'::text, 'transfer'::text, 'debt'::text, 'fee'::text])))` |
| `categories_name_category_type_key` | UNIQUE | `UNIQUE (name, category_type)` |
| `categories_parent_id_fkey` | FOREIGN KEY | `FOREIGN KEY (parent_id) REFERENCES categories(id)` |
| `categories_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |

<a id="table-debt-goals"></a>

## debt_goals

Цели и приоритеты погашения; сами по себе не являются денежными операциями.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `liability_id` | `bigint` | нет | — |
| `target_close_date` | `date` | да | — |
| `target_monthly_payment` | `numeric(14,2)` | да | — |
| `target_extra_payment` | `numeric(14,2)` | да | — |
| `priority` | `smallint` | да | — |
| `rationale` | `text` | да | — |
| `is_active` | `boolean` | нет | `true` |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `debt_goals_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE CASCADE` |
| `debt_goals_liability_id_key` | UNIQUE | `UNIQUE (liability_id)` |
| `debt_goals_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |

<a id="table-debt-payment-details"></a>

## debt_payment_details

Фактическая разбивка платежа на тело, проценты и комиссию.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `transaction_id` | `bigint` | нет | — |
| `liability_id` | `bigint` | нет | — |
| `principal_amount` | `numeric(14,2)` | нет | `0` |
| `interest_amount` | `numeric(14,2)` | нет | `0` |
| `fee_amount` | `numeric(14,2)` | нет | `0` |
| `cash_flow_event_id` | `bigint` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `debt_payment_details_cash_flow_event_id_fkey` | FOREIGN KEY | `FOREIGN KEY (cash_flow_event_id) REFERENCES cash_flow_events(id)` |
| `debt_payment_details_check` | CHECK | `CHECK ((((principal_amount + interest_amount) + fee_amount) > (0)::numeric))` |
| `debt_payment_details_fee_amount_check` | CHECK | `CHECK ((fee_amount >= (0)::numeric))` |
| `debt_payment_details_interest_amount_check` | CHECK | `CHECK ((interest_amount >= (0)::numeric))` |
| `debt_payment_details_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE CASCADE` |
| `debt_payment_details_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `debt_payment_details_principal_amount_check` | CHECK | `CHECK ((principal_amount >= (0)::numeric))` |
| `debt_payment_details_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE CASCADE` |
| `debt_payment_details_transaction_id_key` | UNIQUE | `UNIQUE (transaction_id)` |
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

<a id="table-finance-settings"></a>

## finance_settings

Ключи конфигурации со значениями JSONB.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `key` | `text` | нет | — |
| `value` | `jsonb` | нет | — |
| `notes` | `text` | да | — |
| `updated_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_settings_pkey` | PRIMARY KEY | `PRIMARY KEY (key)` |

<a id="table-liabilities"></a>

## liabilities

Описание долга и связь с кредитным счётом либо платёжным буфером.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `account_id` | `bigint` | нет | — |
| `liability_type` | `text` | нет | — |
| `principal_original` | `numeric(14,2)` | да | — |
| `principal_current` | `numeric(14,2)` | да | — |
| `annual_rate` | `numeric(7,4)` | да | — |
| `minimum_payment` | `numeric(14,2)` | да | — |
| `payment_day` | `smallint` | да | — |
| `term_end_date` | `date` | да | — |
| `interest_free_until` | `date` | да | — |
| `target_close_date` | `date` | да | — |
| `target_extra_payment` | `numeric(14,2)` | да | `0` |
| `priority` | `smallint` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `balance_model` | `text` | нет | — |
| `linked_account_id` | `bigint` | да | — |
| `payment_account_id` | `bigint` | да | — |
| `status` | `text` | нет | `'active'::text` |
| `name` | `text` | нет | — |
| `minimum_payment_calculation_day` | `smallint` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `liabilities_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE CASCADE` |
| `liabilities_balance_model_check` | CHECK | `CHECK (((balance_model IS NULL) OR (balance_model = ANY (ARRAY['revolving_account'::text, 'installment'::text]))))` |
| `liabilities_liability_type_check` | CHECK | `CHECK ((liability_type = ANY (ARRAY['credit_card'::text, 'consumer_loan'::text, 'mortgage'::text, 'other_loan'::text])))` |
| `liabilities_linked_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (linked_account_id) REFERENCES accounts(id)` |
| `liabilities_minimum_payment_calculation_day_check` | CHECK | `CHECK (((minimum_payment_calculation_day IS NULL) OR ((minimum_payment_calculation_day >= 1) AND (minimum_payment_calculation_day <= 31))))` |
| `liabilities_model_accounts` | CHECK | `CHECK ((((balance_model = 'installment'::text) AND (payment_account_id IS NOT NULL) AND (linked_account_id IS NULL)) OR ((balance_model = 'revolving_account'::text) AND (linked_account_id IS NOT NULL))))` |
| `liabilities_payment_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (payment_account_id) REFERENCES accounts(id)` |
| `liabilities_payment_day_check` | CHECK | `CHECK (((payment_day >= 1) AND (payment_day <= 31)))` |
| `liabilities_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `liabilities_status_check` | CHECK | `CHECK ((status = ANY (ARRAY['active'::text, 'closed'::text, 'paused'::text])))` |

<a id="table-liability-balance-snapshots"></a>

## liability_balance_snapshots

Исходные/контрольные суммы тела долга, процентов и комиссий на момент времени.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `liability_id` | `bigint` | нет | — |
| `balance_as_of` | `timestamp with time zone` | нет | — |
| `principal_balance` | `numeric` | нет | — |
| `accrued_interest` | `numeric` | нет | `0` |
| `fees_due` | `numeric` | нет | `0` |
| `source` | `text` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `liability_balance_snapshots_accrued_interest_check` | CHECK | `CHECK ((accrued_interest >= (0)::numeric))` |
| `liability_balance_snapshots_fees_due_check` | CHECK | `CHECK ((fees_due >= (0)::numeric))` |
| `liability_balance_snapshots_liability_id_balance_as_of_key` | UNIQUE | `UNIQUE (liability_id, balance_as_of)` |
| `liability_balance_snapshots_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE CASCADE` |
| `liability_balance_snapshots_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `liability_balance_snapshots_principal_balance_check` | CHECK | `CHECK ((principal_balance >= (0)::numeric))` |

<a id="view-liability-current-balances"></a>

## liability_current_balances

Представление текущих расчётных остатков долгов.

Представление; не отдельная таблица. Выходные поля:

| Поле | SQL-тип |
|---|---|
| `liability_id` | `bigint` |
| `name` | `text` |
| `liability_type` | `text` |
| `balance_model` | `text` |
| `principal_balance` | `numeric` |
| `accrued_interest` | `numeric` |
| `fees_due` | `numeric` |
| `total_debt` | `numeric` |
| `payment_account_id` | `bigint` |
| `linked_account_id` | `bigint` |

<a id="table-liability-movements"></a>

## liability_movements

Движения тела долга, процентов и комиссий; направление задаётся отдельно.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `liability_id` | `bigint` | нет | — |
| `effective_at` | `timestamp with time zone` | нет | — |
| `movement_type` | `text` | нет | — |
| `amount` | `numeric` | нет | — |
| `direction` | `smallint` | нет | `1` |
| `transaction_id` | `bigint` | да | — |
| `cash_flow_event_id` | `bigint` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `debt_payment_detail_id` | `bigint` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `liability_movements_amount_check` | CHECK | `CHECK ((amount > (0)::numeric))` |
| `liability_movements_cash_flow_event_id_fkey` | FOREIGN KEY | `FOREIGN KEY (cash_flow_event_id) REFERENCES cash_flow_events(id) ON DELETE SET NULL` |
| `liability_movements_debt_payment_detail_id_fkey` | FOREIGN KEY | `FOREIGN KEY (debt_payment_detail_id) REFERENCES debt_payment_details(id) ON DELETE CASCADE` |
| `liability_movements_direction_check` | CHECK | `CHECK ((direction = ANY (ARRAY['-1'::integer, 1])))` |
| `liability_movements_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE CASCADE` |
| `liability_movements_movement_type_check` | CHECK | `CHECK ((movement_type = ANY (ARRAY['principal_increase'::text, 'principal_payment'::text, 'interest_accrual'::text, 'interest_payment'::text, 'fee_accrual'::text, 'fee_payment'::text, 'principal_adjustment'::text, 'interest_adjustment'::text, 'fee_adjustment'::text])))` |
| `liability_movements_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `liability_movements_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE CASCADE` |

<a id="table-liability-payment-schedule"></a>

## liability_payment_schedule

Банковский график с разбивкой каждого платежа.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `liability_id` | `bigint` | нет | — |
| `payment_number` | `integer` | да | — |
| `payment_date` | `date` | нет | — |
| `principal_before` | `numeric` | нет | — |
| `principal_amount` | `numeric` | нет | — |
| `interest_amount` | `numeric` | нет | `0` |
| `fee_amount` | `numeric` | нет | `0` |
| `total_amount` | `numeric` | нет | — |
| `principal_after` | `numeric` | да | — |
| `source` | `text` | да | — |
| `source_as_of` | `date` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `updated_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `liability_payment_schedule_components_check` | CHECK | `CHECK ((round(((principal_amount + interest_amount) + fee_amount), 2) = round(total_amount, 2)))` |
| `liability_payment_schedule_fee_amount_check` | CHECK | `CHECK ((fee_amount >= (0)::numeric))` |
| `liability_payment_schedule_interest_amount_check` | CHECK | `CHECK ((interest_amount >= (0)::numeric))` |
| `liability_payment_schedule_liability_date_key` | UNIQUE | `UNIQUE (liability_id, payment_date)` |
| `liability_payment_schedule_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id)` |
| `liability_payment_schedule_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `liability_payment_schedule_principal_amount_check` | CHECK | `CHECK ((principal_amount >= (0)::numeric))` |
| `liability_payment_schedule_principal_before_check` | CHECK | `CHECK ((principal_before >= (0)::numeric))` |
| `liability_payment_schedule_total_amount_check` | CHECK | `CHECK ((total_amount >= (0)::numeric))` |

<a id="table-living-budgets"></a>

## living_budgets

Индивидуальный лимит конкретной недели.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `week_start` | `date` | нет | — |
| `amount` | `numeric` | нет | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `living_budgets_amount_check` | CHECK | `CHECK ((amount >= (0)::numeric))` |
| `living_budgets_monday` | CHECK | `CHECK ((EXTRACT(isodow FROM week_start) = (1)::numeric))` |
| `living_budgets_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `living_budgets_week_start_key` | UNIQUE | `UNIQUE (week_start)` |

<a id="table-planned-purchases"></a>

## planned_purchases

Плановые покупки и их аналитический резерв.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `name` | `text` | нет | — |
| `planned_amount` | `numeric` | нет | — |
| `planned_date` | `date` | да | — |
| `status` | `text` | нет | `'active'::text` |
| `financing_method` | `text` | да | — |
| `actual_transaction_id` | `bigint` | да | — |
| `liability_id` | `bigint` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `closed_at` | `timestamp with time zone` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `planned_purchases_actual_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (actual_transaction_id) REFERENCES transactions(id) ON DELETE SET NULL` |
| `planned_purchases_financing_method_check` | CHECK | `CHECK (((financing_method IS NULL) OR (financing_method = ANY (ARRAY['cash'::text, 'credit_card'::text, 'split'::text, 'other_credit'::text, 'unknown'::text]))))` |
| `planned_purchases_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id) ON DELETE SET NULL` |
| `planned_purchases_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `planned_purchases_planned_amount_check` | CHECK | `CHECK ((planned_amount > (0)::numeric))` |
| `planned_purchases_status_check` | CHECK | `CHECK ((status = ANY (ARRAY['active'::text, 'completed'::text, 'cancelled'::text, 'converted_to_split'::text, 'converted_to_credit'::text])))` |

<a id="table-receipt-items"></a>

## receipt_items

Позиции чека.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('receipt_items_id_seq'::regclass)` |
| `receipt_id` | `bigint` | нет | — |
| `line_number` | `integer` | нет | — |
| `name` | `text` | нет | — |
| `barcode` | `text` | да | — |
| `quantity` | `numeric` | да | — |
| `unit_price` | `numeric` | да | — |
| `amount` | `numeric` | нет | — |
| `category_id` | `bigint` | да | — |
| `confidence` | `numeric` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `receipt_items_amount_check` | CHECK | `CHECK ((amount > (0)::numeric))` |
| `receipt_items_category_id_fkey` | FOREIGN KEY | `FOREIGN KEY (category_id) REFERENCES categories(id)` |
| `receipt_items_confidence_check` | CHECK | `CHECK (((confidence IS NULL) OR ((confidence >= (0)::numeric) AND (confidence <= (1)::numeric))))` |
| `receipt_items_nonnegative_price` | CHECK | `CHECK (((unit_price IS NULL) OR (unit_price >= (0)::numeric)))` |
| `receipt_items_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `receipt_items_positive_quantity` | CHECK | `CHECK (((quantity IS NULL) OR (quantity > (0)::numeric)))` |
| `receipt_items_receipt_id_fkey` | FOREIGN KEY | `FOREIGN KEY (receipt_id) REFERENCES receipts(id) ON DELETE CASCADE` |
| `receipt_items_receipt_id_line_number_key` | UNIQUE | `UNIQUE (receipt_id, line_number)` |

<a id="table-receipts"></a>

## receipts

Чек, связанный с расходной операцией.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('receipts_id_seq'::regclass)` |
| `transaction_id` | `bigint` | нет | — |
| `merchant_name` | `text` | да | — |
| `purchase_at` | `timestamp with time zone` | да | — |
| `total_amount` | `numeric` | нет | — |
| `fiscal_drive_number` | `text` | да | — |
| `fiscal_document_number` | `text` | да | — |
| `fiscal_sign` | `text` | да | — |
| `operation_type` | `smallint` | да | — |
| `qr_payload` | `text` | да | — |
| `source` | `text` | нет | `'photo'::text` |
| `parse_status` | `text` | нет | `'category_only'::text` |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `receipts_parse_status_check` | CHECK | `CHECK ((parse_status = ANY (ARRAY['category_only'::text, 'items_parsed'::text, 'verified_online'::text])))` |
| `receipts_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `receipts_source_check` | CHECK | `CHECK ((source = ANY (ARRAY['photo'::text, 'fns'::text, 'manual'::text])))` |
| `receipts_total_amount_check` | CHECK | `CHECK ((total_amount > (0)::numeric))` |
| `receipts_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE CASCADE` |
| `receipts_transaction_id_key` | UNIQUE | `UNIQUE (transaction_id)` |

<a id="table-transaction-category-allocations"></a>

## transaction_category_allocations

Разбиение одного расхода по категориям.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `nextval('transaction_category_allocations_id_seq'::regclass)` |
| `transaction_id` | `bigint` | нет | — |
| `category_id` | `bigint` | нет | — |
| `amount` | `numeric` | нет | — |
| `allocation_source` | `text` | нет | `'manual'::text` |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `transaction_category_allocations_allocation_source_check` | CHECK | `CHECK ((allocation_source = ANY (ARRAY['manual'::text, 'receipt'::text, 'rule'::text, 'import'::text])))` |
| `transaction_category_allocations_amount_check` | CHECK | `CHECK ((amount > (0)::numeric))` |
| `transaction_category_allocations_category_id_fkey` | FOREIGN KEY | `FOREIGN KEY (category_id) REFERENCES categories(id)` |
| `transaction_category_allocations_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `transaction_category_allocations_transaction_id_category_id_key` | UNIQUE | `UNIQUE (transaction_id, category_id)` |
| `transaction_category_allocations_transaction_id_fkey` | FOREIGN KEY | `FOREIGN KEY (transaction_id) REFERENCES transactions(id) ON DELETE CASCADE` |
| `trg_validate_transaction_category_allocations` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

<a id="table-transactions"></a>

## transactions

Фактические перемещения денег с временной, бюджетной и зарплатной разметкой.

| Поле | SQL-тип | NULL разрешён | Значение по умолчанию / генерация |
|---|---|---|---|
| `id` | `bigint` | нет | `GENERATED BY DEFAULT AS IDENTITY` |
| `transaction_date` | `date` | нет | — |
| `transaction_type` | `text` | нет | — |
| `amount` | `numeric(14,2)` | нет | — |
| `currency` | `text` | нет | `'RUB'::text` |
| `description` | `text` | нет | — |
| `category_id` | `bigint` | да | — |
| `from_account_id` | `bigint` | да | — |
| `to_account_id` | `bigint` | да | — |
| `liability_id` | `bigint` | да | — |
| `external_ref` | `text` | да | — |
| `notes` | `text` | да | — |
| `created_at` | `timestamp with time zone` | нет | `now()` |
| `occurred_at` | `timestamp with time zone` | нет | — |
| `budget_effect` | `text` | нет | — |
| `salary_component` | `text` | да | — |
| `salary_period_start` | `date` | да | — |
| `income_event_id` | `bigint` | да | — |
| `request_payload` | `jsonb` | да | — |
| `reserve_effect` | `text` | нет | `'none'::text` |
| `reserve_cycle` | `text` | да | — |
| `reserve_due_date` | `date` | да | — |
| `reserve_liability_id` | `bigint` | да | — |

### Ограничения

| Имя | Вид | Определение |
|---|---|---|
| `finance_validate_data` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |
| `transactions_amount_check` | CHECK | `CHECK ((amount > (0)::numeric))` |
| `transactions_budget_effect_check` | CHECK | `CHECK ((budget_effect = ANY (ARRAY['life'::text, 'excluded'::text, 'none'::text])))` |
| `transactions_category_id_fkey` | FOREIGN KEY | `FOREIGN KEY (category_id) REFERENCES categories(id)` |
| `transactions_check` | CHECK | `CHECK ((((transaction_type = 'expense'::text) AND (from_account_id IS NOT NULL) AND (to_account_id IS NULL)) OR ((transaction_type = 'income'::text) AND (from_account_id IS NULL) AND (to_account_id IS NOT NULL)) OR ((transaction_type = 'transfer'::text) AND (from_account_id IS NOT NULL) AND (to_account_id IS NOT NULL) AND (from_account_id <> to_account_id)) OR (transaction_type = ANY (ARRAY['debt_payment'::text, 'interest'::text, 'fee'::text, 'adjustment'::text]))))` |
| `transactions_debit_sides` | CHECK | `CHECK (((transaction_type <> ALL (ARRAY['debt_payment'::text, 'interest'::text, 'fee'::text])) OR ((from_account_id IS NOT NULL) AND ((transaction_type = 'debt_payment'::text) OR (to_account_id IS NULL)))))` |
| `transactions_finite_amount` | CHECK | `CHECK ((amount <> 'NaN'::numeric))` |
| `transactions_from_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (from_account_id) REFERENCES accounts(id)` |
| `transactions_income_event_id_fkey` | FOREIGN KEY | `FOREIGN KEY (income_event_id) REFERENCES cash_flow_events(id)` |
| `transactions_income_metadata` | CHECK | `CHECK (((transaction_type = 'income'::text) OR ((income_event_id IS NULL) AND (salary_component IS NULL) AND (salary_period_start IS NULL))))` |
| `transactions_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (liability_id) REFERENCES liabilities(id)` |
| `transactions_life_only_for_expenses` | CHECK | `CHECK (((budget_effect <> 'life'::text) OR (transaction_type = 'expense'::text)))` |
| `transactions_nonempty_sides` | CHECK | `CHECK ((((from_account_id IS NOT NULL) OR (to_account_id IS NOT NULL)) AND ((from_account_id IS NULL) OR (to_account_id IS NULL) OR (from_account_id <> to_account_id))))` |
| `transactions_pkey` | PRIMARY KEY | `PRIMARY KEY (id)` |
| `transactions_reserve_effect_check` | CHECK | `CHECK ((reserve_effect = ANY (ARRAY['none'::text, 'grace_draw'::text, 'grace_repay'::text])))` |
| `transactions_reserve_liability_id_fkey` | FOREIGN KEY | `FOREIGN KEY (reserve_liability_id) REFERENCES liabilities(id)` |
| `transactions_reserve_metadata` | CHECK | `CHECK ((((reserve_effect = 'none'::text) AND (reserve_cycle IS NULL) AND (reserve_due_date IS NULL) AND (reserve_liability_id IS NULL)) OR ((reserve_effect <> 'none'::text) AND (NULLIF(btrim(reserve_cycle), ''::text) IS NOT NULL) AND (reserve_due_date IS NOT NULL) AND (reserve_liability_id IS NOT NULL))))` |
| `transactions_rub` | CHECK | `CHECK ((currency = 'RUB'::text))` |
| `transactions_salary_component_check` | CHECK | `CHECK (((salary_component IS NULL) OR (salary_component = ANY (ARRAY['advance'::text, 'salary'::text, 'vacation'::text, 'bonus'::text, 'other'::text]))))` |
| `transactions_to_account_id_fkey` | FOREIGN KEY | `FOREIGN KEY (to_account_id) REFERENCES accounts(id)` |
| `transactions_transaction_type_check` | CHECK | `CHECK ((transaction_type = ANY (ARRAY['expense'::text, 'income'::text, 'transfer'::text, 'debt_payment'::text, 'interest'::text, 'fee'::text, 'adjustment'::text])))` |
| `trg_validate_grace_reserve_deferred` | t | `TRIGGER DEFERRABLE INITIALLY DEFERRED` |

## Триггеры

Триггеры приведены для навигации по исходникам. Порядок, условия WHEN и отложенность нужно проверять в DDL миграции; ниже сохранено определение из каталога.

| Таблица | Триггер | Определение |
|---|---|---|
| `account_balance_snapshots` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.account_balance_snapshots FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `account_balance_snapshots` | `trg_refresh_snapshot_dependents` | `CREATE TRIGGER trg_refresh_snapshot_dependents AFTER INSERT OR DELETE OR UPDATE ON public.account_balance_snapshots FOR EACH ROW EXECUTE FUNCTION refresh_snapshot_dependents()` |
| `accounts` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.accounts FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `accounts` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.accounts DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `cash_flow_event_funding` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.cash_flow_event_funding FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `cash_flow_event_funding` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.cash_flow_event_funding DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `cash_flow_event_funding` | `trg_apply_event_payment_budget_semantics` | `CREATE TRIGGER trg_apply_event_payment_budget_semantics AFTER INSERT OR UPDATE ON public.cash_flow_event_funding FOR EACH ROW EXECUTE FUNCTION apply_event_payment_budget_semantics()` |
| `cash_flow_event_funding` | `trg_refresh_cash_flow_event_from_allocation` | `CREATE TRIGGER trg_refresh_cash_flow_event_from_allocation AFTER INSERT OR DELETE OR UPDATE ON public.cash_flow_event_funding FOR EACH ROW EXECUTE FUNCTION refresh_cash_flow_event_from_allocation()` |
| `cash_flow_event_funding` | `trg_validate_cash_flow_allocation` | `CREATE TRIGGER trg_validate_cash_flow_allocation BEFORE INSERT OR UPDATE ON public.cash_flow_event_funding FOR EACH ROW EXECUTE FUNCTION validate_cash_flow_allocation()` |
| `cash_flow_events` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.cash_flow_events FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `cash_flow_events` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.cash_flow_events DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `cash_flow_rules` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.cash_flow_rules FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `cash_flow_rules` | `trg_sync_cash_flow_sources` | `CREATE TRIGGER trg_sync_cash_flow_sources AFTER INSERT OR DELETE OR UPDATE ON public.cash_flow_rules FOR EACH STATEMENT EXECUTE FUNCTION sync_cash_flow_sources()` |
| `categories` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.categories FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `debt_goals` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.debt_goals FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `debt_payment_details` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.debt_payment_details FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `debt_payment_details` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.debt_payment_details DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `debt_payment_details` | `trg_sync_debt_payment_movements` | `CREATE TRIGGER trg_sync_debt_payment_movements AFTER INSERT OR DELETE OR UPDATE ON public.debt_payment_details FOR EACH ROW EXECUTE FUNCTION sync_debt_payment_movements()` |
| `debt_payment_details` | `trg_validate_debt_payment_details` | `CREATE TRIGGER trg_validate_debt_payment_details BEFORE INSERT OR UPDATE ON public.debt_payment_details FOR EACH ROW EXECUTE FUNCTION validate_debt_payment_details()` |
| `finance_settings` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.finance_settings FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `liabilities` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.liabilities FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `liabilities` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.liabilities DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `liability_balance_snapshots` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.liability_balance_snapshots FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `liability_balance_snapshots` | `trg_refresh_snapshot_dependents` | `CREATE TRIGGER trg_refresh_snapshot_dependents AFTER INSERT OR DELETE OR UPDATE ON public.liability_balance_snapshots FOR EACH ROW EXECUTE FUNCTION refresh_snapshot_dependents()` |
| `liability_movements` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.liability_movements FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `liability_movements` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.liability_movements DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `liability_movements` | `trg_sync_liability_cache_from_movements` | `CREATE TRIGGER trg_sync_liability_cache_from_movements AFTER INSERT OR DELETE OR UPDATE ON public.liability_movements FOR EACH ROW EXECUTE FUNCTION trg_sync_liability_cache_from_movements()` |
| `liability_payment_schedule` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.liability_payment_schedule FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `liability_payment_schedule` | `trg_sync_cash_flow_sources` | `CREATE TRIGGER trg_sync_cash_flow_sources AFTER INSERT OR DELETE OR UPDATE ON public.liability_payment_schedule FOR EACH STATEMENT EXECUTE FUNCTION sync_cash_flow_sources()` |
| `living_budgets` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.living_budgets FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `planned_purchases` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.planned_purchases FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `receipt_items` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.receipt_items FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `receipt_items` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.receipt_items DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `receipts` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.receipts FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `receipts` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.receipts DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `transaction_category_allocations` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.transaction_category_allocations FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `transaction_category_allocations` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.transaction_category_allocations DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `transaction_category_allocations` | `trg_validate_transaction_category_allocations` | `CREATE CONSTRAINT TRIGGER trg_validate_transaction_category_allocations AFTER INSERT OR DELETE OR UPDATE ON public.transaction_category_allocations DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION validate_transaction_category_allocations_deferred()` |
| `transactions` | `finance_00_write_lock` | `CREATE TRIGGER finance_00_write_lock BEFORE INSERT OR DELETE OR UPDATE ON public.transactions FOR EACH STATEMENT EXECUTE FUNCTION finance_write_lock()` |
| `transactions` | `finance_validate_data` | `CREATE CONSTRAINT TRIGGER finance_validate_data AFTER INSERT OR DELETE OR UPDATE ON public.transactions DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION finance_validate_data_deferred()` |
| `transactions` | `trg_00_normalize_payment_buffer_funding_semantics` | `CREATE TRIGGER trg_00_normalize_payment_buffer_funding_semantics BEFORE INSERT OR UPDATE OF transaction_type, from_account_id, to_account_id, budget_effect ON public.transactions FOR EACH ROW EXECUTE FUNCTION normalize_payment_buffer_funding_semantics()` |
| `transactions` | `trg_clear_credit_card_minimum_allocations` | `CREATE TRIGGER trg_clear_credit_card_minimum_allocations BEFORE DELETE OR UPDATE OF amount, transaction_date, from_account_id, to_account_id, transaction_type ON public.transactions FOR EACH ROW EXECUTE FUNCTION clear_credit_card_minimum_allocations()` |
| `transactions` | `trg_clear_payment_buffer_funding_allocations` | `CREATE TRIGGER trg_clear_payment_buffer_funding_allocations BEFORE DELETE OR UPDATE OF amount, transaction_date, occurred_at, from_account_id, to_account_id, transaction_type, liability_id ON public.transactions FOR EACH ROW EXECUTE FUNCTION clear_payment_buffer_funding_allocations()` |
| `transactions` | `trg_credit_card_payment_semantics` | `CREATE TRIGGER trg_credit_card_payment_semantics BEFORE INSERT OR UPDATE OF transaction_type, amount, from_account_id, to_account_id, liability_id, category_id, budget_effect ON public.transactions FOR EACH ROW EXECUTE FUNCTION normalize_credit_card_payment_semantics()` |
| `transactions` | `trg_normalize_transaction_budget_effect` | `CREATE TRIGGER trg_normalize_transaction_budget_effect BEFORE INSERT OR UPDATE ON public.transactions FOR EACH ROW EXECUTE FUNCTION normalize_transaction_budget_effect()` |
| `transactions` | `trg_reconcile_credit_card_minimum_from_old_transaction` | `CREATE TRIGGER trg_reconcile_credit_card_minimum_from_old_transaction AFTER DELETE OR UPDATE OF amount, transaction_date, occurred_at, from_account_id, to_account_id, transaction_type ON public.transactions FOR EACH ROW EXECUTE FUNCTION reconcile_credit_card_minimum_from_old_transaction()` |
| `transactions` | `trg_reconcile_credit_card_minimum_from_transaction` | `CREATE TRIGGER trg_reconcile_credit_card_minimum_from_transaction AFTER INSERT OR UPDATE OF amount, transaction_date, occurred_at, from_account_id, to_account_id, transaction_type ON public.transactions FOR EACH ROW EXECUTE FUNCTION reconcile_credit_card_minimum_from_transaction()` |
| `transactions` | `trg_reconcile_payment_buffer_funding_from_transaction` | `CREATE TRIGGER trg_reconcile_payment_buffer_funding_from_transaction AFTER INSERT OR UPDATE OF amount, transaction_date, occurred_at, from_account_id, to_account_id, transaction_type, liability_id ON public.transactions FOR EACH ROW EXECUTE FUNCTION reconcile_payment_buffer_funding_from_transaction()` |
| `transactions` | `trg_refresh_transaction_dependents` | `CREATE TRIGGER trg_refresh_transaction_dependents AFTER UPDATE OF amount, occurred_at, transaction_date, transaction_type, from_account_id, to_account_id, liability_id ON public.transactions FOR EACH ROW EXECUTE FUNCTION refresh_transaction_dependents()` |
| `transactions` | `trg_sync_revolving_liability_cache` | `CREATE TRIGGER trg_sync_revolving_liability_cache AFTER INSERT OR DELETE OR UPDATE ON public.transactions FOR EACH ROW EXECUTE FUNCTION trg_sync_revolving_liability_cache()` |
| `transactions` | `trg_sync_salary_transaction_to_event` | `CREATE TRIGGER trg_sync_salary_transaction_to_event AFTER INSERT OR DELETE OR UPDATE OF amount, transaction_type, transaction_date, occurred_at, salary_component, salary_period_start, income_event_id ON public.transactions FOR EACH ROW EXECUTE FUNCTION sync_salary_transaction_to_event()` |
| `transactions` | `trg_validate_grace_reserve` | `CREATE TRIGGER trg_validate_grace_reserve BEFORE INSERT OR UPDATE ON public.transactions FOR EACH ROW EXECUTE FUNCTION validate_grace_reserve()` |
| `transactions` | `trg_validate_grace_reserve_deferred` | `CREATE CONSTRAINT TRIGGER trg_validate_grace_reserve_deferred AFTER INSERT OR DELETE OR UPDATE ON public.transactions DEFERRABLE INITIALLY DEFERRED FOR EACH ROW EXECUTE FUNCTION validate_grace_reserve_deferred()` |

## Доступ

При обследовании RLS выключен на всех 19 таблицах; политики не обнаружены. Это описание текущего состояния, а не рекомендуемые настройки открытой установки. См. [архитектуру](architecture.md).

